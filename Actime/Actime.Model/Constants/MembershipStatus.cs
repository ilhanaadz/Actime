namespace Actime.Model.Constants
{
    public enum MembershipStatus
    {
        Pending = 1,
        Active,
        Suspended,
        Cancelled,
        Expired,
        Rejected
    }
}
