namespace Actime.Model.Constants
{
    public enum AttendanceStatus
    {
        PendingResponse = 1,
        Going,
        Maybe,
        NotGoing,
        Attended,
        Missed
    }
}
