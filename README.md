# Actime

## Credentials

### Desktop - administrator:
- Username: `desktop`  
- Pass: `test`

### Mobile app - basic user:
- Username: `user`  
- Pass: `test`

### Mobile app - organization:
- Username: `organization`  
- Pass: `test`

## Startup
1. Unzip `envValuesZip` (password: `fit`) and place it under `Actime/Actime` (where `docker-compose.yml` is located)
2. Run `docker-compose up`

#### Find `fit-build-2026-02-08.zip` under Actime/Actime/UI
